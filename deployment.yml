apiVersion: apps/v1
kind: Deployment
metadata:
  name: pretendapp
  namespace: identity-demo
spec:
  selector:
    matchLabels:
      app: pretendapp
  template:
    metadata:
      labels:
        app: pretendapp
    spec:
      containers:
      - name: pretend-app
        image: logindotgov/pretend-app:1e00e2552015007bac0403720e70a06a543bf2b1
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
